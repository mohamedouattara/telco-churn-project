{% extends 'base.html.twig' %}

                            {% block title %}Classe{% endblock %}
                            
                            {% block body %}
                            <div class="white-block" style="padding:15px;">
                                <h1>Classe</h1>
                            
                                <table class="table">
                                    <tbody>
                                        <tr>
                                            <th>Annee</th>
                                            <td>{{classe.annee|date('d-m-Y')}}</td>
                                        </tr>
                                        <tr>
                                            <th>Effectif</th>
                                            <td>{{classe.effectif}}</td>
                                        </tr>
                                        <tr>
                                            <th>Professeurprincipal</th>
                                            <td>{{classe.professeurprincipal}}</td>
                                        </tr>
                                        
                                    </tbody>
                                </table>
                                <div class="button-container" >
                                
                                    <a href="{{ path('dataset_index') }}" class="btn">back</a>
                                    <a href="{{ path('classe_edit', {'id': classe.id}) }}" class="btn orange"><i class="material-icons">build</i></a>
                                    {{ include('classe/_delete.html.twig') }}
                                </div>
                            </div>
                            {% endblock %}